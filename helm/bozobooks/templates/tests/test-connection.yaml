apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bozobooks.fullname" . }}-test-connection"
  labels:
    {{- include "bozobooks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bozobooks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
